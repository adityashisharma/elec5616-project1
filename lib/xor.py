class XOR: # Stub class to prevent XOR usage during execution
    def __init__(self, *_, **__):
        raise NotImplementedError(
            "The XOR cipher is deprecated and must not be used. Use AES-CTR + HMAC-SHA512 instead (see lib/comms.py)."
        )
